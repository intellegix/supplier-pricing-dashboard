services:
  - type: web
    name: supplier-pricing-dashboard
    runtime: node
    buildCommand: npm install && npm run build && npm install -g serve
    startCommand: serve -s dist -l 10000
    envVars:
      - key: NODE_VERSION
        value: 20
